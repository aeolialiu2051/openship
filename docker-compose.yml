# ──────────────────────────────────────────────────────────
# Vibrail — Docker Compose (SaaS / from-source control plane)
#
#   cp .env.example .env          # then edit
#   docker compose up -d --build
#
# This is the CONTROL PLANE: it BUILDS api + dashboard + web from source and
# runs postgres + redis. It's what we deploy for the vibrail.warpgateapi.com SaaS, and what
# a from-source contributor runs locally. The MODE is decided entirely by .env —
# no profiles, no per-mode service variants:
#   • SaaS (our env):   CLOUD_MODE=true + VIBRAIL_TARGET=vibrail-saas + Oblien /
#                       GitHub App / secrets. Deployed apps run in Oblien
#                       sandboxes — the control plane never runs them here, so
#                       there is deliberately NO Docker socket and NO edge.
#   • From-source dev:  CLOUD_MODE=false (the .env.example default).
#
# SELF-HOSTING (run your own apps as host containers with the Traefik edge on
# :80/:443) is a DIFFERENT stack — the pull-based images + `edge` + Docker-out-
# of-Docker. That lives in docker/docker-compose.yml (or install it with the
# `vibrail up` CLI). Don't add the edge/socket here; this stays a clean,
# unprivileged control plane.
#
# Services:
#   • postgres → private :5432   (storage)
#   • redis    → private :6379   (queue + cache + rate-limit)
#   • api      → :4000   (override: API_PORT)
#   • dashboard→ :3001   (override: DASHBOARD_PORT)
#   • web      → :3000   (override: WEB_PORT)   landing site (vibrail.warpgateapi.com)
#
# The API + dashboard bind fixed internal ports via PORT (the API honors PORT
# regardless of VIBRAIL_TARGET); a reverse proxy maps the public domains to
# them. postgres/redis are internal-only. The API auto-migrates Postgres on boot.
# The hosted path-routing contract and a Traefik file-provider example live at
# docker/traefik-vibrail-saas.yml:
#   /dashboard/* → dashboard, /api/proxy/* (+ OAuth/MCP/WS) → dashboard,
#   everything else → web.
#
# REMOTE ACCESS: reaching this from another machine (LAN IP or a reverse proxy)
# requires VIBRAIL_PUBLIC_URL in .env — else the dashboard loads but login is
# rejected (403 ORIGIN_REJECTED). Behind a proxy also set TRUST_PROXY=true. Set
# VIBRAIL_BIND_ADDR to publish the ports on one interface instead of all.
# ──────────────────────────────────────────────────────────

services:
  # ─── Database (storage, private) ──────────────────────
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-vibrail}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-vibrail}
      POSTGRES_DB: ${POSTGRES_DB:-vibrail}
    # PRIVATE: not published to the host. api reaches it at postgres:5432.
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-vibrail} -d ${POSTGRES_DB:-vibrail}"]
      interval: 5s
      timeout: 3s
      retries: 12

  # ─── Cache / Queue / Rate-limit (private) ─────────────
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    # PRIVATE: not published to the host. api reaches it at redis:6379.
    expose:
      - "6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 12

  # ─── API (control plane) ──────────────────────────────
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    ports:
      # Publish on all interfaces by default; VIBRAIL_BIND_ADDR pins one.
      - "${VIBRAIL_BIND_ADDR:-0.0.0.0}:${API_PORT:-4000}:${API_PORT:-4000}"
    # Mode + secrets (CLOUD_MODE, DEPLOY_MODE, VIBRAIL_TARGET, Oblien, GitHub
    # App, auth secrets, …) all come from .env. The values below OVERRIDE it
    # with the configured internal port + in-cluster service DNS (compose
    # `environment` wins over `env_file`).
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: "${API_PORT:-4000}"
      VIBRAIL_DASHBOARD_PORT: "${DASHBOARD_PORT:-3001}"
      DATABASE_URL: postgresql://${POSTGRES_USER:-vibrail}:${POSTGRES_PASSWORD:-vibrail}@postgres:5432/${POSTGRES_DB:-vibrail}
      REDIS_URL: redis://redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "bun -e \"fetch('http://127.0.0.1:${API_PORT:-4000}/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 40s

  # ─── Dashboard (app UI) ───────────────────────────────
  dashboard:
    build:
      context: .
      dockerfile: apps/dashboard/Dockerfile
      args:
        # Hosted SaaS shares the marketing origin and mounts the UI beneath
        # /dashboard. Self-hosted/release builds leave this empty and stay at /.
        NEXT_PUBLIC_DASHBOARD_BASE_PATH: "${DASHBOARD_BASE_PATH:-/dashboard}"
    restart: unless-stopped
    ports:
      - "${VIBRAIL_BIND_ADDR:-0.0.0.0}:${DASHBOARD_PORT:-3001}:${DASHBOARD_PORT:-3001}"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: "${DASHBOARD_PORT:-3001}"
      INTERNAL_API_URL: http://api:${API_PORT:-4000}
      # The front server uses the same mount to map root-level API/OAuth/MCP
      # requests into the basePath-aware Next app.
      NEXT_PUBLIC_DASHBOARD_BASE_PATH: "${DASHBOARD_BASE_PATH:-/dashboard}"
    depends_on:
      api:
        condition: service_healthy

  # ─── Web / Landing ────────────────────────────────────
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    restart: unless-stopped
    ports:
      - "${VIBRAIL_BIND_ADDR:-0.0.0.0}:${WEB_PORT:-3000}:${WEB_PORT:-3000}"
    environment:
      NODE_ENV: production
      PORT: "${WEB_PORT:-3000}"
      VIBRAIL_CLOUD_DASHBOARD_URL: "${VIBRAIL_CLOUD_DASHBOARD_URL:-https://vibrail.warpgateapi.com/dashboard}"

volumes:
  postgres_data:
  redis_data:
