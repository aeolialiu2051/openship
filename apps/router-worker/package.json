{
  "name": "@repo/router-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": { "@repo/core": "workspace:*" },
  "devDependencies": { "@cloudflare/workers-types": "^5.20260810.1", "typescript": "^5.9.3", "vitest": "^4.0.18", "wrangler": "^4.30.0" }
}
